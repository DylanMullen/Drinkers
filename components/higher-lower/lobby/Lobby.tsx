import React from 'react'

import styles from './lobby.module.scss'

type Props = {}

function Lobby({}: Props) {
  return (
    <main className={styles["hilo-lobby"]}>
        section
    </main>
  )
}

export default Lobby